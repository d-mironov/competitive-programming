#include <stdio.h>

void rotate(int *nums, int numsSize, int k);

int main() {
    return 0;
}

void rotate(int *nums, int numsSize, int k) {
    if (nums == NULL || k == 0) {
        return;
    }
    for (int i = 0; i < k; i++) {
        
    }
}
